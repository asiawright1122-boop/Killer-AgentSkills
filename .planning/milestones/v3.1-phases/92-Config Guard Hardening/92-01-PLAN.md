# Plan: Phase 92 (Config Guard Hardening)

## Core Tasks

### 1. Composite Config JSON Schema Validation
- **File**: `scripts/lib/ai-config-guard.ts`
- **Action**:
  - Retrieve `AI_OPERATOR_PROFILES_JSON` in `inspectAiConfigGuard()`.
  - Validate that if defined, it is a valid JSON schema conforming to `AICompositeOperatorProfile`.
  - Iterate through keys to check:
    - Environment names must be supported strings.
    - Each workload profile string values must conform to `VALID_OPERATOR_PROFILES`.
  - If invalid, append `invalid_operator_profile` issue.

### 2. Refactor Global Profile Alignment
- **File**: `scripts/lib/ai-config-guard.ts`
- **Action**:
  - Update `rawOperatorProfile` validation check.
  - Rely on `VALID_OPERATOR_PROFILES` instead of hardcoded strings (`nvidia-first`, `workers-ai-fallback`, `openrouter-preferred`) to allow `budget` and `speed` profiles natively.

### 3. Add Config Guard Unit Tests
- **File**: `scripts/lib/ai-config-guard.test.ts`
- **Action**:
  - Write test cases for config guard mapping invalid JSON structures and invalid profile options, asserting correct issue codes and messages are thrown.
