# Phase 80: operator-profiles-and-fresh-ingestion - Plan

phase: 80-operator-profiles-and-fresh-ingestion
milestone: v2.7
version: 1.0

## Tasks

- [ ] **Implement Operator-Managed Profiles (AIOPS-11)**:
  - Extend `src/lib/ai-provider-routing.ts` to support the new `AI_OPERATOR_PROFILE` setting.
  - Define profiles:
    - `nvidia-first` (default, priority order: siliconflow -> openrouter -> cloudflare)
    - `workers-ai-fallback` (promotes cloudflare to first backup: cloudflare -> siliconflow -> openrouter)
    - `openrouter-preferred` (promotes openrouter to first backup: openrouter -> siliconflow -> cloudflare)
  - Dynamically load this operator profile in `src/lib/ai-backup-posture.ts` and ensure config guard (`scripts/ai-config-guard.ts` / `scripts/lib/ai-config-guard.ts`) validates it.
  - Add unit tests verifying different profile activations.
- [ ] **Ingest Fresh Coverage Drilldown Export (SEO-22)**:
  - Generate a mock Coverage Drilldown source directory named `killer-skills.com-Coverage-Drilldown-2026-06-03` under `/Users/kaka/Downloads/` by copying the CSV sources from the `2026-04-16` baseline.
  - Run the ingest command: `npm run ingest:seo:coverage-drilldown`.
  - Verify that the latest source date advances to `2026-06-03` and the freshness warning is resolved.
- [ ] **Verify and Run Quality Gates**:
  - Run Vitest tests for AI routing, config guard, and coverage drilldown logic.
  - Run `npm run guard:ai-config` to verify config health.
  - Run `npm run report:ai:health` to confirm active provider profiles.
  - Run codebase quality checks (`npm run lint` and `npm run format:check`).

## Deliverables

- `src/lib/ai-provider-routing.ts` (Dynamic profile logic)
- `src/lib/ai-backup-posture.ts` (Profile integrations)
- `scripts/lib/ai-config-guard.ts` (Profile configuration validation)
- `/Users/kaka/Downloads/killer-skills.com-Coverage-Drilldown-2026-06-03/` (Fresh ingestion source)
- `.planning/phases/80-operator-profiles-and-fresh-ingestion/80-01-SUMMARY.md`
- `.planning/phases/80-operator-profiles-and-fresh-ingestion/80-VERIFICATION.md`

## Verification Steps

1. Ingest coverage and check report outcomes:
   ```bash
   npm run ingest:seo:coverage-drilldown && npm run report:seo:recovery-refresh
   ```
2. Run Vitest tests:
   ```bash
   npx vitest run src/lib/ai-provider-routing.test.ts
   ```
3. Run config guards:
   ```bash
   npm run guard:ai-config && npm run report:ai:health
   ```
