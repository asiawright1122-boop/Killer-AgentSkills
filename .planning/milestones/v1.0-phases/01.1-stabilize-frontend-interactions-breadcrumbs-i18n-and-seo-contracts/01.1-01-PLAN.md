---
phase: 01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - package.json
  - package-lock.json
  - scripts/dev-server-smoke.mjs
autonomous: true
requirements:
  - BASELINE-01
must_haves:
  truths:
    - "`npm run dev -- --host 127.0.0.1` is restored as the primary local dev entrypoint, and `node scripts/dev-server-smoke.mjs -- npm run dev -- --host 127.0.0.1` proves that script reaches a ready state and exits cleanly without the Cloudflare adapter `./app/manifest` crash."
    - "`npm run build` completes from repo root using the same locked dependency graph."
    - "The phase has one reusable local validation command for the public surface."
  artifacts:
    - path: "package.json"
      provides: "Compatible Astro/cloudflare versions and a reusable validation script"
    - path: "package-lock.json"
      provides: "Locked dependency graph matching the compatible versions"
    - path: "astro.config.mjs"
      provides: "Cloudflare adapter config that works in local dev and build"
    - path: "scripts/dev-server-smoke.mjs"
      provides: "Bounded local readiness probe for the restored `npm run dev` entrypoint"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm dependency resolution"
      pattern: "\"astro\"|\"@astrojs/cloudflare\""
    - from: "package.json"
      to: "astro.config.mjs"
      via: "`npm run dev` and `npm run build` entrypoints"
      pattern: "astro dev|astro build"
    - from: "package.json"
      to: "scripts/dev-server-smoke.mjs"
      via: "bounded startup validation command for the restored `npm run dev` entrypoint"
      pattern: "npm run dev|validate:public-surface|dev-server-smoke"
---

<objective>
Restore a reproducible local Astro/Cloudflare baseline before any frontend behavior work.

Purpose: unblock local debugging and make later interaction/i18n/SEO changes testable.
Output: a restored `npm run dev` contract, working build, plus one reusable validation entrypoint and bounded startup probe for this phase.
</objective>

<execution_context>
@/Users/kaka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kaka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v1.0-phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-CONTEXT.md
@.planning/milestones/v1.0-phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md
@package.json
@astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align Astro and Cloudflare adapter versions with the current config surface</name>
  <files>package.json
package-lock.json
astro.config.mjs
scripts/dev-server-smoke.mjs</files>
  <read_first>package.json
astro.config.mjs
.planning/milestones/v1.0-phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md</read_first>
  <action>Resolve the current `Package subpath './app/manifest'` failure by pinning a compatible `astro` plus `@astrojs/cloudflare` pair and regenerating `package-lock.json`. Preserve the existing SSR externals, `platformProxy` behavior, and production `react-dom/server.edge` alias unless the compatibility fix requires a narrowly scoped adjustment. Add `scripts/dev-server-smoke.mjs` as a bounded readiness probe that invokes `npm run dev -- --host 127.0.0.1`, waits for a successful ready signal on `127.0.0.1`, then exits 0 and tears the child process down cleanly. Use the smoke probe, not an unbounded direct `npm run dev`, as the verification vehicle for the restored package-script contract. Do not leave a mixed adapter/framework pair, a broken lockfile, or a dev validation path that hangs forever.</action>
  <acceptance_criteria>`npm run dev -- --host 127.0.0.1` is the restored local dev contract, `node scripts/dev-server-smoke.mjs -- npm run dev -- --host 127.0.0.1` exits 0 after confirming that contract reaches Astro dev readiness without the `./app/manifest` import error, and `npm run build` succeeds with the Cloudflare adapter enabled.</acceptance_criteria>
  <verify>
    <automated>node scripts/dev-server-smoke.mjs -- npm run dev -- --host 127.0.0.1</automated>
    <automated>npm run build</automated>
  </verify>
  <done>The repo has a stable Astro/Cloudflare dependency pair, `npm run dev` works again as the primary local entrypoint, the dev server can be proven ready with a bounded smoke probe, and the production build works again.</done>
</task>

<task type="auto">
  <name>Task 2: Add one reusable validation entrypoint for the public-surface phase work</name>
  <files>package.json
scripts/dev-server-smoke.mjs</files>
  <read_first>package.json
scripts/dev-server-smoke.mjs
src/i18n.test.ts
src/pages/public-links.test.ts</read_first>
  <action>Add a phase-scoped validation script in `package.json` such as `validate:public-surface` that runs the bounded `npm run dev` smoke, the local build, and the targeted Vitest suites this phase will keep extending. Keep it local and deterministic: bounded readiness probe plus build plus targeted tests only. Do not wire in live-only SEO audits, full pipeline jobs, or unrelated long-running scripts.</action>
  <acceptance_criteria>The repo exposes one command that future plans can rerun to validate the public shell without manually stitching together the build and targeted unit/source tests.</acceptance_criteria>
  <verify>
    <automated>npm run validate:public-surface</automated>
  </verify>
  <done>`package.json` contains a reusable validation command that succeeds after the baseline fix lands.</done>
</task>

</tasks>

<verification>
- `node scripts/dev-server-smoke.mjs -- npm run dev -- --host 127.0.0.1`
- `npm run validate:public-surface`
- `npm run build`
</verification>

<success_criteria>
Local Astro development via `npm run dev` and production build both work from repo root, and later plans can reuse a single validation command instead of reassembling the command surface by hand.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-01-SUMMARY.md`
</output>
