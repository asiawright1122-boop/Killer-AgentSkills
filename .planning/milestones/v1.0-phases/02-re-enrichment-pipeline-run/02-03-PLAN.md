---
phase: 02-re-enrichment-pipeline-run
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - scripts/sync-d1-delta.ts
  - scripts/sync-to-kv.ts
  - reports/seo/phase-02-publish-log.md
autonomous: true
requirements:
  - PUBLISH-01
  - THEME-01
must_haves:
  truths:
    - "Regenerated skill SEO is published through the canonical D1 runtime path."
    - "Supporting KV sync does not masquerade as the authoritative skill-data publish step."
    - "Publish artifacts record what was pushed, skipped, or retried."
  artifacts:
    - path: "reports/seo/phase-02-publish-log.md"
      provides: "Human-readable record of publish steps, retries, and skipped items"
  key_links:
    - from: "scripts/sync-d1-delta.ts"
      to: "data/skills-cache.json"
      via: "runtime skill-data publication"
      pattern: "content_hash|INSERT OR REPLACE|skills"
    - from: "scripts/sync-to-kv.ts"
      to: "package.json"
      via: "supporting docs and sitemap asset sync"
      pattern: "sync:kv"
---

<objective>
Publish the regenerated dataset through the canonical runtime path and clearly separate primary and supporting sync steps.

Purpose: move corrected SEO data into production-facing stores without losing visibility into what was actually published.
Output: canonical D1 publish, supporting KV asset sync where needed, and a publish log artifact.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v1.0-phases/02-re-enrichment-pipeline-run/02-CONTEXT.md
@.planning/milestones/v1.0-phases/02-re-enrichment-pipeline-run/02-02-PLAN.md
@scripts/sync-d1-delta.ts
@scripts/sync-to-kv.ts
@package.json
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Publish regenerated skill data to D1</name>
  <files>scripts/sync-d1-delta.ts
reports/seo/phase-02-publish-log.md</files>
  <action>Run the canonical D1 publish step for the regenerated dataset and record batch-level publish outcomes, including skipped statements, retries, and any reduced oversized payloads. The publish log should make it obvious what reached the runtime store and what still needs attention.</action>
  <acceptance_criteria>The regenerated skill data is published through D1 with a clear log of retries, skips, and batch behavior.</acceptance_criteria>
  <verify>
    <automated>npm run sync:d1:delta</automated>
  </verify>
  <done>The canonical runtime store reflects the regenerated dataset.</done>
</task>

<task type="auto">
  <name>Task 2: Run supporting KV sync without conflating it with the canonical publish path</name>
  <files>scripts/sync-to-kv.ts
reports/seo/phase-02-publish-log.md</files>
  <action>Run supporting KV sync only for the assets it still owns, and record its outcome separately from the D1 publish result so later readers do not mistake KV sync for the authoritative skill-data deploy step.</action>
  <acceptance_criteria>Supporting KV sync completes or skips cleanly, and its role stays explicitly secondary to D1 publication.</acceptance_criteria>
  <verify>
    <automated>npm run sync:kv</automated>
  </verify>
  <done>Supporting KV assets are synchronized without obscuring the canonical D1 publish result.</done>
</task>

</tasks>

<verification>
- `npm run sync:d1:delta`
- `npm run sync:kv`
</verification>

<success_criteria>
The corrected dataset is published through D1, auxiliary KV assets are synchronized appropriately, and the phase leaves behind a clear publish log.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/02-re-enrichment-pipeline-run/02-03-SUMMARY.md`
</output>
