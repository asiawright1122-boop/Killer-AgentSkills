---
wave: 2
depends_on: ["05-01"]
files_modified:
  - "src/islands/CommandPalette.tsx"
  - "src/islands/HeaderActions.tsx"
  - "src/components/Header.astro"
autonomous: true
---

# Phase 6: Search Deep Integration & Global Trigger

<objective>
Wire the newly created Command Palette internally to the real `/api/search.ts` backend API with request bouncing (Debounce logic), and expose a static visual "Search... ⌘K" trigger button directly in the main navigation header component.
</objective>

<task>
<read_first>
- src/islands/CommandPalette.tsx
- src/islands/HeaderActions.tsx
- src/components/Header.astro
</read_first>
<action>
1. Update `CommandPalette.tsx` to include an async `useEffect` that debounces user input arrays and safely fetches data from `/api/search.ts?q=...`. Discard the dummy mock placeholders and map over the real backend backend JSON layout `[{ title, description, url }]`. Hook the `Enter` keystroke to execute an immediate `window.location.href` to the bounded skill link URL.
2. Update `HeaderActions.tsx` (OR inside `Header.astro` relying locally if logic separates) to explicitly include a "Search... ⌘K" button mimicking the styling dimensions of exactly the existing `Submit Skill` Neo-Brutalist button. Pressing this trigger dispatches a custom event to un-hide the `CommandPalette`.
</action>
<acceptance_criteria>
- Rapidly typing into the palette conditionally triggers a 300ms+ debounced network request to the backend.
- The primary Navigation Header displays the new visual search trigger visually aligned with sibling buttons.
- Clicking the visual search trigger successfully forces the modal to visually open precisely.
</acceptance_criteria>
</task>

<verification>
<must_haves>
- The physical search query MUST correctly fetch real operational responses utilizing the pre-built backend endpoints without crashing the AST hydration.
- The search results node layout must naturally respond to Enter physical hardware jumps.
</must_haves>
</verification>
