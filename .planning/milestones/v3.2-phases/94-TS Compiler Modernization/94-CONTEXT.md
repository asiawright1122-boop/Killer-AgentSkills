# Context: Phase 94 (TS Compiler Modernization)

This phase aims to resolve typescript compiler configuration deprecations and align tsconfig files across workspaces (`workers/` and `packages/cli/`).
