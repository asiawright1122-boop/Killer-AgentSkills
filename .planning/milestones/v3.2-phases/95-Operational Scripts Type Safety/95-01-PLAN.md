# Plan: Phase 95 (Operational Scripts Type Safety)

## Core Tasks
- Create `scripts/tsconfig.json` mapping rules for `scripts/` directory.
- Remediate typescript errors in execution queues, delta boards, telemetry reporting, and SEO testing files.
- Run `npx tsc --project scripts/tsconfig.json --noEmit` to verify type safety.
