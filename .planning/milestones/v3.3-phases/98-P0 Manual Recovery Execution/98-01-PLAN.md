# Plan: Phase 98 (P0 Manual Recovery Execution)

## Core Tasks
- Run the AI-driven thin content enrichment script to expand priority skills with bodies < 500 characters:
  ```bash
  npx tsx scripts/ai-enrich-thin-skills.ts
  ```
- Rebuild the skills cache database to apply these updates to the edge-ready schema:
  ```bash
  npx tsx scripts/build-skills-cache.ts --mode=discover
  ```
- Verify that `data/skills-cache.json` gets updated with rich Markdown documentation.
