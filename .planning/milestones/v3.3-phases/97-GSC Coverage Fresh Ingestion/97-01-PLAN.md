# Plan: Phase 97 (GSC Coverage Fresh Ingestion)

## Core Tasks
- Locate a fresh GSC Coverage data source (such as `data/mock-gsc-coverage.csv` which has mock dates from June 2026, or update the crawl dates in `data/coverage-drilldown-raw/killer-skills.com-Coverage-Drilldown-2026-06-03/table.csv`).
- Ingest the fresh coverage file into the local D1 database using:
  ```bash
  npx tsx scripts/ingest-coverage-drilldown.ts <path-to-fresh-csv>
  ```
- Run build-time or validation checks to ensure the 7-day SLA freshness validation rule passes without warnings.
