---
phase: 02-re-enrichment-pipeline-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/build-skills-cache.ts
  - scripts/seo-index-integrity.ts
  - reports/seo/index-drift.json
  - reports/seo/phase-02-regeneration-baseline.md
autonomous: true
requirements:
  - THEME-01
  - REPORT-01
  - RESUME-01
must_haves:
  truths:
    - "The team can quantify exactly which skills are queued for SEO regeneration and why."
    - "Phase 2 begins with a resumable inventory, not a blind full-corpus rerun."
    - "Baseline artifacts capture drift, thin-content risk, and representative samples before any publish step."
  artifacts:
    - path: "reports/seo/phase-02-regeneration-baseline.md"
      provides: "Before-state counts, sample IDs, and rerun scope for the regeneration pass"
    - path: "reports/seo/index-drift.json"
      provides: "Current sitemap/indexable-cache drift evidence used for spot checks"
  key_links:
    - from: "scripts/build-skills-cache.ts"
      to: "src/lib/shared/validation.ts"
      via: "theme gating and non-target filtering"
      pattern: "POSITIVE_THEME_KEYWORDS|getNonTargetSkillReason|isSkillFullyOptimized"
    - from: "scripts/seo-index-integrity.ts"
      to: "data/skills-cache.json"
      via: "baseline integrity and thin-content checks"
      pattern: "strict|fail-on-thin|fail-on-missing-body"
---

<objective>
Establish the precise regeneration scope before any publish step.

Purpose: turn Phase 2 into a measured, resumable pipeline run by producing a dry-run quality baseline, affected-skill inventory, and sample set for later spot checks.
Output: baseline report artifacts and a clear rerun scope for the flagged skill subset.
</objective>

<execution_context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-re-enrichment-pipeline-run/02-CONTEXT.md
@package.json
@scripts/build-skills-cache.ts
@scripts/seo-index-integrity.ts
@reports/seo/index-drift.json
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Quantify the flagged regeneration set and baseline risk</name>
  <files>scripts/build-skills-cache.ts
scripts/seo-index-integrity.ts
reports/seo/phase-02-regeneration-baseline.md</files>
  <action>Add or formalize a dry-run/report-first path that identifies the skills failing the new theme gates, summarizes why they are failing, and records representative IDs for spot checks. The output should separate theme failures, thin-content risks, and missing-body risks so later batches can be prioritized and resumed safely.</action>
  <acceptance_criteria>The phase can answer "how many skills will change, why, and which samples should be manually inspected" before any publish step begins.</acceptance_criteria>
  <verify>
    <automated>npm run audit:seo:index-quality</automated>
    <automated>node --import tsx scripts/seo-index-integrity.ts</automated>
  </verify>
  <done>Baseline counts, sample IDs, and rerun scope are written to report artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Define resumable batch boundaries for the regeneration pass</name>
  <files>scripts/build-skills-cache.ts
reports/seo/phase-02-regeneration-baseline.md</files>
  <action>Choose and document the batch boundaries for the flagged subset so reruns can resume without reprocessing everything. Record the ordering rule, any checkpoint file or progress artifact, and the criteria for considering a batch safe to publish.</action>
  <acceptance_criteria>The regeneration pass has a deterministic execution order and restart strategy, and the plan leaves behind enough information for a later run to resume instead of starting from scratch.</acceptance_criteria>
  <verify>
    <automated>npm run audit:seo:index-quality</automated>
  </verify>
  <done>Phase 2 has a concrete batch plan and checkpoint strategy.</done>
</task>

</tasks>

<verification>
- `npm run audit:seo:index-quality`
- `node --import tsx scripts/seo-index-integrity.ts`
</verification>

<success_criteria>
Phase 2 starts from a quantified, auditable, and resumable baseline instead of a blind regeneration run.
</success_criteria>

<output>
After completion, create `.planning/phases/02-re-enrichment-pipeline-run/02-01-SUMMARY.md`
</output>
