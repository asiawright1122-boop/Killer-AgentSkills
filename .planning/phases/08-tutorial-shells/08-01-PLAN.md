---
wave: 1
depends_on: []
files_modified:
  - "src/pages/skills/[slug].astro"
  - "src/components/TutorialShell.astro"
autonomous: true
---

# Phase 8: Deploy Astro Edge Tutorial Shells

<objective>
Refactor the skill dynamic detail pages (`src/pages/skills/[slug].astro`) to natively utilize Astro Edge (SSR) capabilities. Inject a Neo-Brutalist `TutorialShell.astro` structure that elegantly renders the massive AI-generated documentation (Harvested in Phase 7) utilizing sticky sidebars, auto-generated Table of Contents, and syntax-highlighted markdown blocks.
</objective>

<task>
<read_first>
- src/data/skills-cache.json
</read_first>
<action>
1. Establish `src/components/TutorialShell.astro` featuring a 2-column or 3-column layout: Left (Navigation/TOC), Center (Markdown Content), Right (Metadata/Actions).
2. Wire Astro SSR logic `export const prerender = false` in `[slug].astro` to pull the dynamically updated data streams on request execution.
3. Automatically parse the Markdown strings (`body`) into safe HTML with a Markdown component that injects Neo-Brutalist styling (heavy borders on code blocks, bright link shadows).
</action>
<acceptance_criteria>
- The dynamic route successfully reads and hydrates the newly grown AI JSON tree at runtime.
- A functional Table of Contents automatically anchors to `## Usage` / `## Architecture`.
- Production build AST validation passes gracefully under `npm run check:astro`.
</acceptance_criteria>
</task>

<verification>
<must_haves>
- The `TutorialShell` MUST avoid hydration mismatches with any injected React islands (like Command Palette or Favorite buttons).
- Dynamic Edge routes MUST safely 404 on missing slugs rather than throwing internal AST fatals.
</must_haves>
</verification>
