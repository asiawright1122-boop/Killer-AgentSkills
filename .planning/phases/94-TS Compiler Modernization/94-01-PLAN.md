# Plan: Phase 94 (TS Compiler Modernization)

## Core Tasks
- Resolve baseUrl deprecation warning in `workers/tsconfig.json` (TS5101).
- Resolve moduleResolution deprecation warning in `packages/cli/tsconfig.json` (TS5107).
- Ensure `npx tsc --noEmit` runs without warnings or errors in both workspaces.
