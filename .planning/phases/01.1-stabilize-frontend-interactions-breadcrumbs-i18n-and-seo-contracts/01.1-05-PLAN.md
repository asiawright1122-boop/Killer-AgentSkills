---
phase: 01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts
plan: 05
type: execute
wave: 4
depends_on:
  - 01.1-04
files_modified:
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/Pagination.astro
  - src/pages/[locale]/index.astro
  - src/pages/[locale]/skills/index.astro
autonomous: true
requirements:
  - I18N-01
must_haves:
  truths:
    - "Shared shell components, the homepage, and the skills index render explicit fallback copy instead of raw translation keys."
    - "Common public-shell copy falls back intentionally when translations are missing instead of leaking raw keys through island or page props."
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Header labels resolved through explicit i18n fallback helpers"
    - path: "src/components/Footer.astro"
      provides: "Footer labels and newsletter controls resolved through explicit i18n fallback helpers"
    - path: "src/components/Pagination.astro"
      provides: "Shared pagination copy resolved through explicit i18n fallback helpers"
    - path: "src/pages/[locale]/index.astro"
      provides: "Homepage copy paths resolved through explicit i18n fallback helpers"
    - path: "src/pages/[locale]/skills/index.astro"
      provides: "Skills index copy paths resolved through explicit i18n fallback helpers"
  key_links:
    - from: "src/components/Header.astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/components/Footer.astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/components/Pagination.astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/pages/[locale]/index.astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/pages/[locale]/skills/index.astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
---

<objective>
Eliminate the highest-traffic public-shell key leakage before the broader route-level cleanup and smoke coverage pass.

Purpose: make the shared shell, homepage, and skills index match the explicit locale contract so the most visible public surfaces stop depending on implicit fallback behavior.
Output: helper adoption across shell, homepage, and skills index.
</objective>

<execution_context>
@/Users/kaka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kaka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-CONTEXT.md
@.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md
@src/components/Header.astro
@src/components/Footer.astro
@src/components/Pagination.astro
@src/pages/[locale]/index.astro
@src/pages/[locale]/skills/index.astro
@src/layouts/Layout.astro
@src/i18n.ts
@src/pages/public-links.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace implicit fallback paths in shared shell components with explicit helper-driven copy resolution</name>
  <files>src/components/Header.astro
src/components/Footer.astro
src/components/Pagination.astro</files>
  <read_first>src/components/Header.astro
src/components/Footer.astro
src/components/Pagination.astro
src/i18n.ts
.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md</read_first>
  <action>Update the header, footer, and pagination component so every public label that currently relies on `t('...') || fallback` uses the explicit helper from `src/i18n.ts`. Resolve the label before passing it into React island props, use a deliberate English fallback only when the translation is actually missing, and explicitly fix known leak-prone paths such as collections/docs/cli/integrations labels, footer email and subscribe controls, and shared pagination copy.</action>
  <acceptance_criteria>The shared shell components no longer render raw translation keys on missing-copy paths, and island props cannot receive unresolved key strings by accident.</acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The shared shell uses explicit fallback behavior instead of leaking key names.</done>
</task>

<task type="auto">
  <name>Task 2: Replace implicit fallback paths on the homepage and skills index</name>
  <files>src/pages/[locale]/index.astro
src/pages/[locale]/skills/index.astro</files>
  <read_first>src/pages/[locale]/index.astro
src/pages/[locale]/skills/index.astro
src/layouts/Layout.astro
src/i18n.ts
src/pages/public-links.test.ts
.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md</read_first>
  <action>Update the homepage and skills index so visible copy or metadata-adjacent copy paths that currently rely on `t('...') || fallback` use the explicit helper contract instead. Keep the existing copy choices and route behavior intact while removing the structural leak path from high-traffic public directory surfaces such as home `seoIntro` and marketplace-facing labels.</action>
  <acceptance_criteria>The homepage and skills index no longer contain leak-prone implicit fallback patterns for visible copy or metadata-adjacent labels.</acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run test -- src/pages/public-links.test.ts</automated>
  </verify>
  <done>The homepage and skills index no longer depend on implicit `t('...') || fallback` behavior.</done>
</task>

</tasks>

<verification>
- `npm run build`
- `npm run test -- src/pages/public-links.test.ts`
</verification>

<success_criteria>
The shared shell, homepage, and skills index no longer leak raw keys, and the highest-traffic public directory surfaces use the explicit i18n helper contract.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-05-SUMMARY.md`
</output>
