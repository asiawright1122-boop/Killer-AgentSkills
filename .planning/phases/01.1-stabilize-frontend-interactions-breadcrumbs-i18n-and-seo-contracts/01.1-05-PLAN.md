---
phase: 01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts
plan: 05
type: execute
wave: 4
depends_on:
  - 01.1-04
files_modified:
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/pages/[locale]/index.astro
  - src/pages/[locale]/skills/index.astro
  - src/pages/[locale]/skills/[owner]/[...repo].astro
  - src/pages/public-links.test.ts
  - scripts/seo-smoke.ts
autonomous: true
requirements:
  - I18N-01
  - REGRESSION-01
must_haves:
  truths:
    - "Header, footer, home, marketplace, and the representative detail page render explicit fallback copy instead of raw translation keys."
    - "Repo-level tests fail if touched public components reintroduce raw key output or bypass the shared builders."
    - "Repo/local checks cover `/en`, `/zh`, `/en/skills`, `/en/collections`, and a representative detail page, and a live audit path exists for post-deploy drift detection on those surfaces."
    - "Repo/local checks assert title, meta description, canonical, hreflang, breadcrumb parity, and locale-metadata alignment on the touched public pages."
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Header labels resolved through explicit i18n fallback helpers"
    - path: "src/components/Footer.astro"
      provides: "Footer labels and newsletter controls resolved through explicit i18n fallback helpers"
    - path: "src/pages/[locale]/index.astro"
      provides: "Homepage copy paths resolved through explicit i18n fallback helpers"
    - path: "src/pages/[locale]/skills/index.astro"
      provides: "Skills index copy paths resolved through explicit i18n fallback helpers"
    - path: "src/pages/[locale]/skills/[owner]/[...repo].astro"
      provides: "Representative detail-page copy paths resolved through explicit i18n fallback helpers"
    - path: "src/pages/public-links.test.ts"
      provides: "Repo-level guards against raw-key leakage and builder bypass"
    - path: "scripts/seo-smoke.ts"
      provides: "Parameterized smoke assertions for raw-key leakage and metadata/title/locale drift on touched pages, reusable for local checks and post-deploy audits"
  key_links:
    - from: "src/components/Header.astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/components/Footer.astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/pages/[locale]/index.astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/pages/[locale]/skills/index.astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/pages/[locale]/skills/[owner]/[...repo].astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "scripts/seo-smoke.ts"
      to: "https://killer-skills.com"
      via: "live smoke path for touched pages"
      pattern: "/en|/zh|/collections|/skills"
---

<objective>
Eliminate the remaining public-shell key leakage and add smoke guards for repo/live drift on the touched pages.

Purpose: make the visible public pages in the smoke/UAT surface match the locale contract and ensure drift can be audited after deploy, not only in repo tests.
Output: helper adoption across shell plus target pages, source-level regression guards, local smoke checks, and a reusable post-deploy audit path for raw keys plus metadata alignment.
</objective>

<execution_context>
@/Users/kaka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kaka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-CONTEXT.md
@.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md
@src/components/Header.astro
@src/components/Footer.astro
@src/pages/[locale]/index.astro
@src/pages/[locale]/skills/index.astro
@src/pages/[locale]/skills/[owner]/[...repo].astro
@src/i18n.ts
@src/pages/public-links.test.ts
@scripts/seo-smoke.ts
@reports/seo/index-drift.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace raw-key fallback paths on the smoke-scope public pages with explicit helper-driven copy resolution</name>
  <files>src/components/Header.astro
src/components/Footer.astro
src/pages/[locale]/index.astro
src/pages/[locale]/skills/index.astro
src/pages/[locale]/skills/[owner]/[...repo].astro</files>
  <read_first>src/components/Header.astro
src/components/Footer.astro
src/pages/[locale]/index.astro
src/pages/[locale]/skills/index.astro
src/pages/[locale]/skills/[owner]/[...repo].astro
src/layouts/Layout.astro
src/i18n.ts
.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md</read_first>
  <action>Update the header, footer, homepage, skills index, and representative detail page so every public label or metadata-adjacent copy path in the smoke/UAT surface that currently relies on `t('...') || fallback` uses the explicit helper from `src/i18n.ts`. Resolve the label before passing it into React island props or layout props, use a deliberate English fallback only when the translation is actually missing, and explicitly fix known leak-prone paths such as home `seoIntro`, shell labels for collections/docs/cli/integrations, footer email and subscribe controls, and target-page labels on the marketplace and detail surface.</action>
  <acceptance_criteria>The touched smoke-scope pages no longer render raw translation keys on missing-copy paths, and shell/page props cannot receive unresolved key strings by accident.</acceptance_criteria>
  <verify>
    <automated>npm run test -- src/pages/public-links.test.ts</automated>
  </verify>
  <done>The touched public pages in the smoke/UAT surface use explicit fallback behavior instead of leaking key names.</done>
</task>

<task type="auto">
  <name>Task 2: Extend repo/local smoke checks and define a reusable post-deploy audit path for raw keys, breadcrumbs, titles, descriptions, and locale metadata drift</name>
  <files>src/pages/public-links.test.ts
scripts/seo-smoke.ts</files>
  <read_first>src/pages/public-links.test.ts
scripts/seo-smoke.ts
src/layouts/Layout.astro
src/pages/[locale]/index.astro
src/pages/[locale]/skills/index.astro
src/pages/[locale]/skills/[owner]/[...repo].astro
src/pages/[locale]/collections/index.astro
src/pages/[locale]/collections/[...slug].astro
reports/seo/index-drift.json</read_first>
  <action>Extend `src/pages/public-links.test.ts` with source-based guards that fail when touched public components or pages reintroduce `t('...') ||` fallback patterns or bypass the shared breadcrumb/metadata builders. Then expand `scripts/seo-smoke.ts` so the smoke path covers `/en`, `/zh`, `/en/skills`, `/en/collections`, and one concrete detail page, asserting no raw translation keys, correct title and meta description output, correct canonical and hreflang output, locale metadata alignment (`html[lang]`, Open Graph locale or equivalent touched metadata), and breadcrumb JSON-LD that matches the visible breadcrumb labels. Keep repo/local verification as the execution gate, and document the same script invocation against `https://killer-skills.com` as a post-deploy audit path rather than a blocking completion gate. Use `reports/seo/index-drift.json` only as evidence to choose a useful live surface, not as an output to rewrite.</action>
  <acceptance_criteria>The repo-level guard fails on raw-key leakage or builder bypass, the smoke script can catch copy/title/locale/breadcrumb contract regressions on local or preview targets, and the phase leaves behind a documented post-deploy audit path for production drift.</acceptance_criteria>
  <verify>
    <automated>npm run test -- src/pages/public-links.test.ts</automated>
  </verify>
  <done>Public-page regressions are covered by repo/local checks, and production drift can be audited with the same smoke tooling after deploy.</done>
</task>

</tasks>

<verification>
- `npm run test -- src/pages/public-links.test.ts`
</verification>

<success_criteria>
No touched public shell surface leaks raw keys, the repo plus local smoke checks catch source regressions for the pages this phase stabilizes, and the phase leaves behind a reusable post-deploy audit path for live drift.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-05-SUMMARY.md`
</output>
