# Plan 01 Summary

## Outcome
- Restored the local Astro/Cloudflare baseline by aligning `@astrojs/cloudflare` back to `12.6.12`, which matches the repo's `astro@5.17.1` contract.
- Added a reusable bounded startup probe in `scripts/dev-server-smoke.mjs`.
- Added `npm run validate:public-surface` so this phase can rerun one deterministic local validation command.

## Files Changed
- `package.json`
- `package-lock.json`
- `scripts/dev-server-smoke.mjs`

## Verification
- `npm run dev -- --host 127.0.0.1 --port 4321`
  - Astro dev now reaches ready state instead of crashing on `Package subpath './app/manifest'`.
- `node scripts/dev-server-smoke.mjs`
  - Passed when run outside the sandbox's local-port restriction.
- `npm run build`
  - Passed with the Cloudflare adapter enabled.
- `npm run validate:public-surface`
  - Passed end-to-end: dev smoke, build, and targeted Vitest suites.

## Notes
- Build/dev now surface non-blocking warnings that should be handled in later plans:
  - duplicate blog content IDs during Astro content sync
  - CSS minifier warning for the generated `[file:line]` style token
- The smoke script now uses an ephemeral port by default so it does not collide with an already-running dev server.
