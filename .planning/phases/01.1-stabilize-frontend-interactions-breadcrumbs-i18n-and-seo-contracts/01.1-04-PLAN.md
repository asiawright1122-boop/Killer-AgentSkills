---
phase: 01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts
plan: 04
type: execute
wave: 3
depends_on:
  - 01.1-02
  - 01.1-03
files_modified:
  - src/lib/site/breadcrumbs.ts
  - src/lib/site/breadcrumbs.test.ts
  - src/lib/site/metadata.ts
  - src/lib/site/metadata.test.ts
  - src/layouts/Layout.astro
  - src/components/Breadcrumb.astro
  - src/pages/[locale]/collections/index.astro
  - src/pages/[locale]/collections/[...slug].astro
autonomous: true
requirements:
  - SEO-01
  - SEO-02
must_haves:
  truths:
    - "Touched pages build breadcrumb UI and breadcrumb JSON-LD from the same item list."
    - "Canonical, hreflang, x-default, title, meta description, and locale metadata for touched pages come from one shared metadata builder."
    - "Touched pages keep metadata locale markers aligned with the served locale and visible copy contract."
    - "Collections pages no longer invent their own breadcrumb or alternate-locale rules inline."
  artifacts:
    - path: "src/lib/site/breadcrumbs.ts"
      provides: "Shared breadcrumb item and JSON-LD builders"
    - path: "src/lib/site/metadata.ts"
      provides: "Shared canonical/hreflang/x-default/title/description/locale metadata builder"
    - path: "src/layouts/Layout.astro"
      provides: "Layout-level metadata wiring backed by the shared builder"
    - path: "src/pages/[locale]/collections/[...slug].astro"
      provides: "Collection detail page using shared breadcrumb and metadata contracts"
  key_links:
    - from: "src/layouts/Layout.astro"
      to: "src/lib/site/metadata.ts"
      via: "canonical/hreflang/x-default/title/description/locale tag generation"
      pattern: "canonical|hreflang|description|og:locale|twitter:title|twitter:description"
    - from: "src/pages/[locale]/collections/index.astro"
      to: "src/lib/site/breadcrumbs.ts"
      via: "shared breadcrumb item list and JSON-LD"
      pattern: "buildBreadcrumb"
    - from: "src/pages/[locale]/collections/[...slug].astro"
      to: "src/lib/site/breadcrumbs.ts"
      via: "shared breadcrumb item list and JSON-LD"
      pattern: "buildBreadcrumb"
---

<objective>
Unify the breadcrumb and SEO metadata contract for layout and collections surfaces behind tested builders.

Purpose: remove duplicated breadcrumb/canonical/title/description logic and make visible navigation, JSON-LD, metadata copy, and locale metadata agree.
Output: shared breadcrumb and metadata builder modules plus layout and collection pages refactored to use them.
</objective>

<execution_context>
@/Users/kaka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kaka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-CONTEXT.md
@.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md
@src/layouts/Layout.astro
@src/components/Breadcrumb.astro
@src/pages/[locale]/collections/index.astro
@src/pages/[locale]/collections/[...slug].astro
@src/i18n.ts

<interfaces>
From `src/components/Breadcrumb.astro`:
```ts
interface BreadcrumbItem {
  label: string;
  href?: string;
}
```

From `src/layouts/Layout.astro`:
```ts
interface Props {
  locale: string;
  title: string;
  description?: string;
  keywords?: string;
  image?: string;
  customCanonical?: string;
  availableLocales?: string[];
  xDefaultLocale?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared breadcrumb and metadata helpers with unit tests</name>
  <files>src/lib/site/breadcrumbs.ts
src/lib/site/breadcrumbs.test.ts
src/lib/site/metadata.ts
src/lib/site/metadata.test.ts</files>
  <read_first>src/components/Breadcrumb.astro
src/layouts/Layout.astro
src/pages/[locale]/collections/index.astro
src/pages/[locale]/collections/[...slug].astro
src/i18n.ts</read_first>
  <action>Create `src/lib/site/breadcrumbs.ts` to own the breadcrumb item shape and the matching `BreadcrumbList` JSON-LD payload, and create `src/lib/site/metadata.ts` to own title, meta description, canonical URL, hreflang alternates, x-default resolution, and locale metadata mapping for touched public pages. Add focused unit tests for breadcrumb position ordering, visible-label to JSON-LD parity, trailing-slash normalization, canonical-locale selection, alternate-locale generation, title/description normalization, and BCP-47 to page metadata locale alignment.</action>
  <acceptance_criteria>The new builder tests prove that one breadcrumb input can power both UI and JSON-LD, and one metadata input can power title, description, canonical, alternates, and locale metadata for the touched page types.</acceptance_criteria>
  <verify>
    <automated>npm run test -- src/lib/site/breadcrumbs.test.ts src/lib/site/metadata.test.ts</automated>
  </verify>
  <done>The breadcrumb and metadata contracts exist as tested reusable builders instead of inline page snippets.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor layout and collections pages to consume the shared builders</name>
  <files>src/layouts/Layout.astro
src/components/Breadcrumb.astro
src/pages/[locale]/collections/index.astro
src/pages/[locale]/collections/[...slug].astro</files>
  <read_first>src/lib/site/breadcrumbs.ts
src/lib/site/metadata.ts
src/layouts/Layout.astro
src/components/Breadcrumb.astro
src/pages/[locale]/collections/index.astro
src/pages/[locale]/collections/[...slug].astro
src/i18n.ts</read_first>
  <action>Replace the inline canonical/hreflang builder in `Layout.astro` with the shared metadata helper, and refactor the collections index/detail pages to construct one breadcrumb item array that feeds both `Breadcrumb.astro` and the JSON-LD script payload. Keep collection canonical-locale and noindex behavior intact, but remove duplicated page-local breadcrumb assembly and alternate-locale string building. While touching `Layout.astro`, route metadata fields through the shared helper so title, description, canonical, hreflang, x-default, and locale markers are resolved together, and route any fallback copy through the new explicit translation helper rather than `t('...') || fallback`.</action>
  <acceptance_criteria>`Layout.astro` no longer computes alternates inline, both collection pages emit the same breadcrumb labels in the visible UI and the JSON-LD payload, and the shared metadata helper owns title/description plus locale alignment for touched pages.</acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run test -- src/lib/site/breadcrumbs.test.ts src/lib/site/metadata.test.ts</automated>
  </verify>
  <done>The layout and collection pages now share one breadcrumb/metadata contract instead of maintaining duplicate logic.</done>
</task>

</tasks>

<verification>
- `npm run build`
- `npm run test -- src/lib/site/breadcrumbs.test.ts src/lib/site/metadata.test.ts`
</verification>

<success_criteria>
Breadcrumb UI, breadcrumb JSON-LD, title/description metadata, canonical URLs, and hreflang/locale tags for the touched collections surfaces are generated from shared builders and no longer drift independently.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-04-SUMMARY.md`
</output>
