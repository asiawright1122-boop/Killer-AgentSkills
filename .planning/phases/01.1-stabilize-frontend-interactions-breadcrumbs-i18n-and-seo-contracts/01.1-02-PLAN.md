---
phase: 01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - src/components/CardOverlayLink.astro
  - src/components/SkillCard.astro
  - src/components/CollectionCard.astro
  - src/pages/[locale]/collections/index.astro
  - src/islands/HeaderActions.tsx
  - playwright.config.ts
  - tests/e2e/navigation.spec.ts
autonomous: true
requirements:
  - PUBLIC-CLICK-01
  - REGRESSION-01
must_haves:
  truths:
    - "Desktop skill and collection cards navigate on click without dead zones."
    - "Closing the mobile menu restores page clickability immediately."
    - "Navigation regression tests assert real click-through behavior on local pages, including locale switching on desktop and mobile."
  artifacts:
    - path: "src/components/CardOverlayLink.astro"
      provides: "Single overlay-anchor primitive for public cards"
    - path: "src/islands/HeaderActions.tsx"
      provides: "Overlay teardown that does not leave pointer interception behind"
    - path: "tests/e2e/navigation.spec.ts"
      provides: "Click-through navigation regression coverage"
    - path: "playwright.config.ts"
      provides: "Local Playwright harness for this phase"
  key_links:
    - from: "src/components/CardOverlayLink.astro"
      to: "src/components/SkillCard.astro"
      via: "shared stretched-link model"
      pattern: "CardOverlayLink"
    - from: "src/components/CardOverlayLink.astro"
      to: "src/components/CollectionCard.astro"
      via: "shared stretched-link model"
      pattern: "CardOverlayLink"
    - from: "src/islands/HeaderActions.tsx"
      to: "tests/e2e/navigation.spec.ts"
      via: "mobile menu open/close, locale-switch, and route-change assertions"
      pattern: "toggleMenu|closeMenu|switchLanguage"
---

<objective>
Normalize public click behavior across shared cards and the mobile navigation overlay.

Purpose: remove the user-visible "almost unclickable" failure mode and cover it with local E2E checks.
Output: one shared card overlay primitive, hardened mobile menu teardown, and click-through Playwright coverage.
</objective>

<execution_context>
@/Users/kaka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kaka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-CONTEXT.md
@.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md
@src/components/SkillCard.astro
@src/components/CollectionCard.astro
@src/pages/[locale]/collections/index.astro
@src/islands/HeaderActions.tsx
@playwright.config.ts
@tests/e2e/navigation.spec.ts

<interfaces>
From `src/islands/HeaderActions.tsx`:
```tsx
interface HeaderActionsProps {
  locale: string;
  localeNames: Record<string, string>;
  labels: {
    home: string;
    skills: string;
    categories: string;
    docs: string;
    blog: string;
    cli: string;
    community: string;
    collections: string;
    language: string;
    theme: string;
    favorites: string;
    github: string;
    switchLanguage: string;
    toggleTheme: string;
    toggleMenu: string;
    favoritesAria: string;
    darkMode?: string;
    lightMode?: string;
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace page-local stretched-link variants with one shared card overlay primitive</name>
  <files>src/components/CardOverlayLink.astro
src/components/SkillCard.astro
src/components/CollectionCard.astro
src/pages/[locale]/collections/index.astro</files>
  <read_first>src/components/SkillCard.astro
src/components/CollectionCard.astro
src/pages/[locale]/collections/index.astro
.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md</read_first>
  <action>Create `src/components/CardOverlayLink.astro` as the single absolute-anchor primitive for public cards, then migrate skill cards, collection cards, and collection index cards to it. The primitive must own the focus ring, anchor stacking, and pointer-event defaults. Keep only explicit CTA controls above the overlay with deliberate `relative`/`z-index`/`pointer-events-auto` usage; remove ad hoc per-page anchor layering and `z-0`/`z-1` variants that make hit areas inconsistent.</action>
  <acceptance_criteria>All touched cards share the same overlay-link structure, their primary click target covers the card body, and nested CTA controls remain intentionally clickable without blocking navigation.</acceptance_criteria>
  <verify>
    <automated>npm run test -- src/pages/public-links.test.ts</automated>
  </verify>
  <done>Public card surfaces use one predictable click model instead of repeated local hacks.</done>
</task>

<task type="auto">
  <name>Task 2: Harden mobile overlay teardown and make Playwright assert real click navigation</name>
  <files>src/islands/HeaderActions.tsx
playwright.config.ts
tests/e2e/navigation.spec.ts</files>
  <read_first>src/islands/HeaderActions.tsx
playwright.config.ts
tests/e2e/navigation.spec.ts
src/layouts/Layout.astro</read_first>
  <action>Update `HeaderActions.tsx` so the mobile menu always clears `document.body.style.overflow`, closes on navigation and locale switch, and leaves the hidden overlay root in a fully inert `pointer-events-none` state. Then rework `playwright.config.ts` and `tests/e2e/navigation.spec.ts` to run locally against the restored dev server, remove the hardcoded `devUrl` constant, and replace DOM-presence checks with click-through assertions for desktop nav, collection-card navigation, skill-card navigation, desktop locale switching, and mobile menu open-close-resume plus locale-switch interaction.</action>
  <acceptance_criteria>The local Playwright run boots the app automatically, can click through the critical public journeys including locale switching, and fails if the overlay still blocks interaction after close, route change, or locale change.</acceptance_criteria>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/navigation.spec.ts</automated>
  </verify>
  <done>Interaction regressions are covered by local click-through E2E tests instead of passive DOM checks.</done>
</task>

</tasks>

<verification>
- `npm run test:e2e -- tests/e2e/navigation.spec.ts`
</verification>

<success_criteria>
The public navigation shell and shared cards are reliably clickable on desktop and mobile, and the local E2E suite proves it by following real user paths including locale switching.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-02-SUMMARY.md`
</output>
