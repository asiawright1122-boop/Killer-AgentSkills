---
phase: 01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts
plan: 06
type: execute
wave: 5
depends_on:
  - 01.1-05
files_modified:
  - src/pages/[locale]/skills/[owner]/[...repo].astro
  - src/pages/[locale]/docs/[...slug].astro
  - src/pages/[locale]/blog/[...slug].astro
  - src/pages/[locale]/blog/category/[category].astro
  - src/pages/[locale]/blog/index.astro
  - src/pages/[locale]/solutions/[topic].astro
  - src/messages/public-copy.test.ts
  - src/pages/public-links.test.ts
  - scripts/seo-smoke.ts
autonomous: true
requirements:
  - I18N-01
  - SEO-01
  - SEO-02
  - REGRESSION-01
must_haves:
  truths:
    - "The representative detail route and the remaining public content routes no longer rely on implicit fallback behavior or leak raw translation keys."
    - "The representative detail route emits breadcrumb and head metadata that stay aligned with the shared contracts used on other touched public pages."
    - "Repo-level guards fail if touched public routes reintroduce raw-key leakage or bypass the shared breadcrumb and metadata builders."
    - "A parameterized smoke script covers `/en`, `/zh`, `/en/skills`, `/en/collections`, and a representative detail page for local verification, and the same script can be reused for post-deploy drift audits."
    - "Repo guards plus the smoke script assert title, meta description, canonical, hreflang, breadcrumb parity, and locale-metadata alignment on the touched public pages."
  artifacts:
    - path: "src/pages/[locale]/skills/[owner]/[...repo].astro"
      provides: "Representative detail-page copy plus breadcrumb/metadata inputs resolved through shared helpers"
    - path: "src/pages/[locale]/docs/[...slug].astro"
      provides: "Docs route copy paths resolved through explicit i18n fallback helpers"
    - path: "src/pages/[locale]/blog/index.astro"
      provides: "Blog route copy paths resolved through explicit i18n fallback helpers"
    - path: "src/pages/[locale]/solutions/[topic].astro"
      provides: "Solutions route copy paths resolved through explicit i18n fallback helpers"
    - path: "src/messages/public-copy.test.ts"
      provides: "Source-level public-copy guards for touched public routes"
    - path: "src/pages/public-links.test.ts"
      provides: "Repo-level guards against raw-key leakage and builder bypass"
    - path: "scripts/seo-smoke.ts"
      provides: "Parameterized smoke assertions for raw-key leakage and metadata/title/locale drift on touched pages, including self-starting local-target support and post-deploy audits"
  key_links:
    - from: "src/pages/[locale]/skills/[owner]/[...repo].astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/pages/[locale]/skills/[owner]/[...repo].astro"
      to: "src/lib/site/breadcrumbs.ts"
      via: "shared breadcrumb contract for representative detail route"
      pattern: "buildBreadcrumb"
    - from: "src/pages/[locale]/skills/[owner]/[...repo].astro"
      to: "src/lib/site/metadata.ts"
      via: "shared metadata contract for representative detail route"
      pattern: "canonical|hreflang|title|description|metadata"
    - from: "src/pages/[locale]/docs/[...slug].astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/pages/[locale]/blog/index.astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/pages/[locale]/solutions/[topic].astro"
      to: "src/i18n.ts"
      via: "explicit translation fallback helper"
      pattern: "translateOr|hasTranslation"
    - from: "src/pages/public-links.test.ts"
      to: "src/lib/site/metadata.ts"
      via: "source guards for builder-backed metadata usage"
      pattern: "metadata|breadcrumb|translateOr|hasTranslation"
    - from: "scripts/seo-smoke.ts"
      to: "package.json"
      via: "self-started local smoke target using the repo dev entrypoint"
      pattern: "npm run dev|seo:smoke"
    - from: "scripts/seo-smoke.ts"
      to: "https://killer-skills.com"
      via: "post-deploy drift audit path for touched pages"
      pattern: "/en|/zh|/collections|/skills"
---

<objective>
Finish the remaining route-level i18n cleanup and lock the phase down with repo/local smoke guards plus a reusable post-deploy audit path.

Purpose: remove the remaining implicit fallback leak paths from the public content/detail routes and make drift detectable before and after deploy.
Output: remaining route cleanup, source-level guards, local smoke checks, and a reusable post-deploy audit path for raw keys plus metadata alignment.
</objective>

<execution_context>
@/Users/kaka/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kaka/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-CONTEXT.md
@.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md
@src/pages/[locale]/skills/[owner]/[...repo].astro
@src/pages/[locale]/docs/[...slug].astro
@src/pages/[locale]/blog/[...slug].astro
@src/pages/[locale]/blog/category/[category].astro
@src/pages/[locale]/blog/index.astro
@src/pages/[locale]/solutions/[topic].astro
@src/layouts/Layout.astro
@src/i18n.ts
@src/messages/public-copy.test.ts
@src/pages/public-links.test.ts
@scripts/seo-smoke.ts
@reports/seo/index-drift.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace implicit fallback paths on the representative detail route and remaining public content routes</name>
  <files>src/pages/[locale]/skills/[owner]/[...repo].astro
src/pages/[locale]/docs/[...slug].astro
src/pages/[locale]/blog/[...slug].astro
src/pages/[locale]/blog/category/[category].astro
src/pages/[locale]/blog/index.astro
src/pages/[locale]/solutions/[topic].astro</files>
  <read_first>src/pages/[locale]/skills/[owner]/[...repo].astro
src/pages/[locale]/docs/[...slug].astro
src/pages/[locale]/blog/[...slug].astro
src/pages/[locale]/blog/category/[category].astro
src/pages/[locale]/blog/index.astro
src/pages/[locale]/solutions/[topic].astro
src/layouts/Layout.astro
src/i18n.ts
src/lib/site/breadcrumbs.ts
src/lib/site/metadata.ts
.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/PRD.md</read_first>
  <action>Update the representative detail route plus the remaining public route templates currently using `t('...') || fallback` semantics so they use the explicit helper contract instead of relying on the key string as a falsey check. For the representative detail route, also route breadcrumb and head-metadata inputs through the shared builders from Plan 04 instead of leaving page-local contract assembly in place. Keep existing copy choices and route behavior intact while removing the structural leak path from public templates that are already part of the current public experience.</action>
  <acceptance_criteria>The currently identified public detail and content routes no longer contain leak-prone implicit fallback patterns for visible copy or metadata-adjacent labels, and the representative detail route no longer bypasses the shared breadcrumb/metadata contracts.</acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The remaining public detail and content routes no longer depend on implicit `t('...') || fallback` behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Extend repo/local smoke checks and define a reusable post-deploy audit path for raw keys, breadcrumbs, titles, descriptions, and locale metadata drift</name>
  <files>src/messages/public-copy.test.ts
src/pages/public-links.test.ts
scripts/seo-smoke.ts</files>
  <read_first>src/messages/public-copy.test.ts
src/pages/public-links.test.ts
scripts/seo-smoke.ts
src/layouts/Layout.astro
src/pages/[locale]/index.astro
src/pages/[locale]/skills/index.astro
src/pages/[locale]/skills/[owner]/[...repo].astro
src/pages/[locale]/docs/[...slug].astro
src/pages/[locale]/blog/[...slug].astro
src/pages/[locale]/blog/category/[category].astro
src/pages/[locale]/blog/index.astro
src/pages/[locale]/solutions/[topic].astro
src/pages/[locale]/collections/index.astro
src/pages/[locale]/collections/[...slug].astro
reports/seo/index-drift.json</read_first>
  <action>Extend `src/messages/public-copy.test.ts` and `src/pages/public-links.test.ts` with source-based guards that fail when touched public components or pages reintroduce `t('...') ||` fallback patterns or bypass the shared breadcrumb/metadata builders. Then expand `scripts/seo-smoke.ts` so the smoke path covers `/en`, `/zh`, `/en/skills`, `/en/collections`, and one concrete detail page, asserting no raw translation keys, correct title and meta description output, correct canonical and hreflang output, locale metadata alignment (`html[lang]`, Open Graph locale or equivalent touched metadata), and breadcrumb JSON-LD that matches the visible breadcrumb labels. Keep repo/local verification as the execution gate by giving the smoke script a self-starting local mode that launches `npm run dev -- --host 127.0.0.1`, waits for readiness, runs the assertions against `http://127.0.0.1:4321`, and tears the process down cleanly. Document the same script invocation against `https://killer-skills.com` as a post-deploy audit path rather than a blocking completion gate. Use `reports/seo/index-drift.json` only as evidence to choose a useful live surface, not as an output to rewrite.</action>
  <acceptance_criteria>The repo-level guards fail on raw-key leakage or builder bypass, the smoke script can self-start a local verification target before deploy and run the touched-page assertions deterministically, and the phase leaves behind a documented post-deploy audit path for production drift.</acceptance_criteria>
  <verify>
    <automated>npm run test -- src/messages/public-copy.test.ts src/pages/public-links.test.ts</automated>
    <automated>npm run seo:smoke -- http://127.0.0.1:4321 --spawn-dev</automated>
  </verify>
  <done>Public-page regressions are covered by repo/local checks, and production drift can be audited with the same smoke tooling after deploy.</done>
</task>

</tasks>

<verification>
- `npm run build`
- `npm run test -- src/messages/public-copy.test.ts src/pages/public-links.test.ts`
- `npm run seo:smoke -- http://127.0.0.1:4321 --spawn-dev`
- Post-deploy audit: `npm run seo:smoke -- https://killer-skills.com`
</verification>

<success_criteria>
No currently identified public route or shared shell surface leaks raw keys, the repo plus local smoke checks catch source regressions for the pages this phase stabilizes, and the phase leaves behind a reusable post-deploy audit path for live drift.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-stabilize-frontend-interactions-breadcrumbs-i18n-and-seo-contracts/01.1-06-SUMMARY.md`
</output>
