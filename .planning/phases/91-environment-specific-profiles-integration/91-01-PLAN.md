# Plan: Phase 91 (Environment-Specific Profiles Integration)

## Core Tasks

### 1. Composite Profile Definition & Parser
- **File**: `src/lib/ai-backup-posture.ts`
- **Action**:
  - Add types `AIEnvironmentName = 'production' | 'development' | 'testing'` and Composite Profile interface mapping environment + workload class to an `AIOperatorProfileName`.
  - Write `parseCompositeOperatorProfile(raw: string)` logic.
  - Implement `resolveAIOperatorProfile` which checks `process.env.AI_OPERATOR_PROFILES_JSON`, fallback to environment (`process.env.NODE_ENV`), then workload type, and falls back to standard `parseAIOperatorProfile(process.env.AI_OPERATOR_PROFILE)` if composite config is missing.

### 2. Update Backup Routing Logic
- **File**: `src/lib/ai-provider-routing.ts`
- **Action**:
  - Update `getBackupPriorityOrderForWorkload` signature to accept options: `{ workloadProfile: AIProviderWorkloadProfileName, envName?: string, customOperatorProfile?: AIOperatorProfileName }`.
  - Integrate dynamic profile resolution inside `buildProviderRoutingPlan`, using `resolveAIOperatorProfile` to lookup active settings.

### 3. Basic Test Scaffold Setup
- **File**: `src/lib/ai-provider-routing.test.ts` (or similar new test runner)
- **Action**:
  - Add mock environment variables testing blocks verifying profile priority routing order under dev vs prod mock settings.
