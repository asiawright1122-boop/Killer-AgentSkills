# Plan: Phase 99 (Technical Scorecard Recalculation)

## Core Tasks
- Recalculate the technical recovery scorecard using the fresh inputs and newly rich cache:
  ```bash
  npx tsx scripts/seo-recovery-scorecard.ts
  ```
- Review the generated scorecard reports (`reports/seo/latest-recovery-scorecard.json` and `reports/seo/latest-recovery-scorecard.md`) to verify that `technicalRecoveryStatus` is correctly updated and reflects the new state.
