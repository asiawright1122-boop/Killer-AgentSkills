# Phase 74: limited-rollout-evidence-and-promotion - Context

## Background

Under Milestone `v2.2 Directory Automated Expansion and Full Rollout`, we are tracking the 5 primary authority surfaces that were promoted to `limited-rollout` in the prior milestone via operator override.

This phase (Phase 74) is focused on:
1. Monitoring target P0 surfaces to ensure their rankings and indexing status remain stable.
2. Extending the operator override switch (`OVERRIDE_EXPANSION_BOUNDARY=open`) in the experiment ladder to allow P0 limited-rollout experiments to be promoted to `automation-candidate` status.
3. Refreshing the GSC and SEO recovery stack reports to confirm the transition.

## Active Constraints

- Direct click indicators remain thin/zero due to GSC latency and organic flow return time.
- The promotion to `automation-candidate` is authorized through the operator override flag to unblock milestone progress.
