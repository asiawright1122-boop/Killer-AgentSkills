# Phase 78: directory-analytics-and-scaling-escalation - Plan

phase: 78-directory-analytics-and-scaling-escalation
milestone: v2.5
version: 1.0

## Tasks

- [x] **Implement Server-Side Analytics Tracking (SEO-21)**:
  - Create a new analytics helper `src/lib/analytics.ts` that provides `trackDirectoryView` function.
  - Implement server-side collection parameters (capturing IP country, User-Agent, locale, and target repository details).
  - Add unit tests for `analytics.ts` formatting and event payloads in `src/lib/analytics.test.ts`.
  - Integrate `trackDirectoryView` inside the directory rendering path: `src/pages/[locale]/skills/[owner]/[...repo].astro`.
- [x] **Implement Request-Density Monitor and Alert Rules (REC-39)**:
  - Extend the edge middleware `src/middleware.ts` to implement sliding-window request-density logging and evaluation.
  - Introduce an alert trigger when request density spikes pass limit thresholds.
  - Implement a static-fallback routing bypass (serving reference-only HTML directly) under extreme density load to preserve CPU limits.
  - Add tests in `src/middleware.property.test.ts` or a new test suite to verify request density spike handling and autoscale alert logging.
- [x] **Run Quality Gates**:
  - Run all Vitest unit tests (`npm test`).
  - Run ESLint and Prettier checks (`npm run lint` and `npm run format:check`).
- [x] **Generate Phase Closeout Deliverables**:
  - Create `78-01-SUMMARY.md` and `78-VERIFICATION.md` in the phase directory.
- [x] **Closeout Milestone v2.5**:
  - Update `ROADMAP.md` and `STATE.md` to flag Phase 78 complete and Milestone v2.5 ready for archive.
  - Run `npm run report:planning:milestones` to ensure clean lifecycle indicators.

## Deliverables

- `src/lib/analytics.ts`
- `src/lib/analytics.test.ts`
- `.planning/milestones/v2.5-phases/78-directory-analytics-and-scaling-escalation/78-01-SUMMARY.md`
- `.planning/milestones/v2.5-phases/78-directory-analytics-and-scaling-escalation/78-VERIFICATION.md`

## Verification Steps

1. Run the new analytics and middleware unit tests:
   ```bash
   npx vitest run src/lib/analytics.test.ts src/middleware.property.test.ts
   ```
2. Run codebase quality checks:
   ```bash
   npm run lint && npm run format:check
   ```
