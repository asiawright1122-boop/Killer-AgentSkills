---
wave: 1
depends_on: []
files_modified:
  - "src/islands/CommandPalette.tsx"
autonomous: true
---

# Phase 5: Command Palette Core UI

<objective>
Implement the modal `CommandPalette.tsx` React component featuring Neo-Brutalism aesthetics (heavy structural borders, offset shadows), complete with `Cmd+K` keyboard event listener registration and internal up/down arrow navigation logic.
</objective>

<task>
<read_first>
- src/islands/SubmitSkillModal.tsx
</read_first>
<action>
Create `src/islands/CommandPalette.tsx`. 
- State: Manage `isOpen`, `query`, and `selectedIndex`.
- Effect: Add a global `keydown` listener on `window` for `Cmd+K` (or `Ctrl+K`) to toggle `isOpen`. Call `e.preventDefault()` to override the browser's default URL focus behavior.
- UI: Render a dark, glassmorphic or Neo-Brutalist overlay. Render a big, clear input field at the top.
- Navigation: Add `ArrowUp` and `ArrowDown` handlers to adjust `selectedIndex`.
- Mock Data: Temporarily render dummy results such as "热门 Workflow: Auto-Blog" down below.
</action>
<acceptance_criteria>
- `CommandPalette.tsx` exports a valid React functional component.
- The component correctly toggles visual visibility when pressing `Cmd+K`.
- The Up/Down keyboard events specifically intersect and correctly update the selected mathematical index state limit.
</acceptance_criteria>
</task>

<verification>
<must_haves>
- The palette MUST be hidden natively by default on load, and successfully un-hide when intercepting the global keyboard shortcut sequence without console errors.
</must_haves>
</verification>
