# Phase 79: index-integrity-and-ai-posture-hardening - Plan

phase: 79-index-integrity-and-ai-posture-hardening
milestone: v2.6
version: 1.0

## Tasks

- [x] **Hardening AI Posture configurations (AIOPS-12)**:
  - Add `AI_BACKUP_SILICONFLOW_POSTURE=disabled` in `.env.local` to override SiliconFlow provider status.
  - Implement dynamic `.env.local` loading inside config guard (`scripts/ai-config-guard.ts`).
  - Refresh AI provider telemetry and run health checks to verify that SiliconFlow is disabled and NVIDIA is preferred.
- [x] **Sitemap and Cache Index Integrity Alignment (REC-40)**:
  - Analyze the index divergence between sitemap generation and indexable cache.
  - Update `scripts/seo-corpus-governance.ts` to enforce that route inclusion is strictly bound to `canonicalLocale` indexability and `isPublicSkillForSitemap` check.
  - Append the missing 6 indexable candidates in `scripts/seo-corpus-governance.ts` with normalized route formats.
- [x] **Run Verification and Quality Gates**:
  - Run `npm run audit:seo:index-integrity` to verify zero index drift (`onlyInSitemap=0`, `onlyInIndexableCache=0`).
  - Run `npm run guard:ai-config` to verify AI config state.
  - Run `npm run report:ai:health` to check provider status.
  - Run code quality gates (`npm run lint` and `npm run format:check`).
- [x] **Generate Phase Closeout Deliverables**:
  - Create `79-01-SUMMARY.md` and `79-VERIFICATION.md` in the phase directory.
- [x] **Closeout Milestone v2.6**:
  - Update `ROADMAP.md` and `STATE.md` to flag Phase 79 complete.

## Deliverables

- `.env.local` (SiliconFlow fallback configuration disablement)
- `scripts/ai-config-guard.ts` (Dynamic `.env.local` overrides check)
- `scripts/seo-corpus-governance.ts` (Strict index alignment check and candidates fix)
- `.planning/phases/79-index-integrity-and-ai-posture-hardening/79-01-SUMMARY.md`
- `.planning/phases/79-index-integrity-and-ai-posture-hardening/79-VERIFICATION.md`

## Verification Steps

1. Verify index integrity:
   ```bash
   npm run audit:seo:index-integrity
   ```
2. Verify AI config health:
   ```bash
   npm run guard:ai-config && npm run report:ai:health
   ```
3. Run codebase quality checks:
   ```bash
   npm run lint && npm run format:check
   ```
