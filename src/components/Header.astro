---
import HeaderActions from '../islands/HeaderActions';
import { LOCALE_NAMES } from '../i18n';

interface Props {
	locale: string;
	t: (key: string) => string;
}

const { locale, t } = Astro.props;
---

<header class="sticky top-0 z-50 w-full border-b border-slate-200/80 dark:border-slate-800/80 bg-white/80 dark:bg-slate-950/80 backdrop-blur-xl transition-all">
    <div class="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between">
        
        <!-- Logo -->
        <a href={`/${locale}`} class="flex items-center gap-2 group">
            <div class="relative w-8 h-8 flex items-center justify-center group-hover:scale-105 transition-transform">
                <!-- Outer Cyan Glow Ring -->
                <div class="absolute inset-0 rounded-full border-[1.5px] border-cyan-400 shadow-[0_0_8px_rgba(34,211,238,0.6),inset_0_0_8px_rgba(34,211,238,0.6)]"></div>
                <!-- Top Pink Dot -->
                <div class="absolute -top-1 left-1/2 -translate-x-1/2 w-2 h-2 rounded-full bg-fuchsia-400 shadow-[0_0_6px_rgba(232,121,249,0.8)]"></div>
                <!-- Inner Hexagon-like Cutout with Text -->
                <div class="relative w-[85%] h-[85%] bg-gradient-to-br from-cyan-400 to-fuchsia-500 flex items-center justify-center text-[10px] font-black tracking-tighter text-transparent bg-clip-text" style="clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%); background: #050505;">
                    <span class="z-10 bg-gradient-to-br from-cyan-400 to-fuchsia-400 text-transparent bg-clip-text" style="-webkit-text-stroke: 0.5px rgba(34,211,238,0.8); letter-spacing: -1px;">KS</span>
                    <!-- Border trick for the hexagon -->
                    <div class="absolute inset-[1px] bg-slate-950 dark:bg-slate-950" style="clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);"></div>
                </div>
            </div>
            <span class="font-bold text-xl tracking-tight bg-clip-text text-transparent bg-gradient-to-r from-slate-800 to-slate-600 dark:from-white dark:to-slate-300">
                Killer-Skills
            </span>
        </a>

        <!-- Desktop Nav -->
        <nav class="hidden md:flex items-center gap-8 text-sm font-medium text-slate-600 dark:text-slate-400">
            <a href={`/${locale}`} class="hover:text-cyan-600 dark:hover:text-cyan-400 transition-colors">{t('Common.home')}</a>
            <a href={`/${locale}/skills`} class="hover:text-cyan-600 dark:hover:text-cyan-400 transition-colors">{t('Common.skillsMarket')}</a>
            <a href={`/${locale}/blog`} class="hover:text-cyan-600 dark:hover:text-cyan-400 transition-colors">{t('Navigation.blog')}</a>
            <a href={`/${locale}/docs`} class="hover:text-cyan-600 dark:hover:text-cyan-400 transition-colors">{t('Footer.docs')}</a>
            <a href={`/${locale}/cli`} class="hover:text-cyan-600 dark:hover:text-cyan-400 transition-colors">{t('Footer.cli')}</a>
        </nav>

        <!-- Rights/Actions -->
        <HeaderActions 
            client:idle 
            locale={locale} 
            localeNames={LOCALE_NAMES} 
            labels={{
                home: t('Navigation.home'),
                skills: t('Navigation.marketplace'),
                categories: t('Navigation.categories'),
                blog: t('Navigation.blog'),
                docs: t('Navigation.docs'),
                cli: t('Footer.cli'),
                community: t('Footer.community'),
                language: t('Common.language'),
                theme: t('Common.theme') || 'Theme',
                favorites: t('Navigation.favorites'),
                github: t('Navigation.github'),
                switchLanguage: t('Aria.switchLanguage'),
                toggleTheme: t('Aria.toggleTheme'),
                toggleMenu: t('Aria.toggleMenu'),
                favoritesAria: t('Navigation.favorites')
            }}
        />

    </div>
</header>
