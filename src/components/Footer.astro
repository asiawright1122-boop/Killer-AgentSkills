---
interface Props {
	locale: string;
	t: (key: string) => string;
    compact?: boolean;
}

const { locale, t, compact = false } = Astro.props;
const currentYear = new Date().getFullYear();
---

<footer class={`border-t border-slate-200 dark:border-slate-800 bg-slate-50/50 dark:bg-slate-950/50 ${compact ? 'py-8' : 'pt-16 pb-8'} ${compact ? 'mt-0' : ''}`}>
    <div class="max-w-7xl mx-auto px-6">
        {!compact && (
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 mb-16">
                
                {/* Brand */}
                <div class="space-y-4">
                    <a href={`/${locale}`} class="flex items-center gap-2 group">
                        <span class="font-black text-2xl tracking-tight uppercase text-[var(--foreground)]">
                            Killer-Skills
                        </span>
                    </a>
                    <p class="text-[var(--muted-foreground)] leading-relaxed text-sm font-medium">
                        {t('Footer.desc')}
                    </p>
                    <div class="flex gap-4">

                        <a href="https://twitter.com/killerskills" aria-label="Follow us on X (Twitter)" target="_blank" rel="noopener noreferrer" class="text-slate-400 hover:text-cyan-600 dark:hover:text-cyan-400 transition-colors">
                            <svg aria-hidden="true" focusable="false" class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
                        </a>
                    </div>
                </div>

                {/* Product */}
                <div>
                    <h4 class="font-black text-[var(--foreground)] mb-4 uppercase tracking-wider">{t('Footer.product')}</h4>
                    <ul class="space-y-3 text-sm text-[var(--muted-foreground)] font-bold">
                        <li><a href={`/${locale}/skills`} class="hover:text-[var(--primary)] hover:underline decoration-2 underline-offset-4 transition-all">{t('Footer.browse')}</a></li>
                        <li><a href={`/${locale}/categories`} class="hover:text-[var(--primary)] hover:underline decoration-2 underline-offset-4 transition-all">{t('Footer.categories')}</a></li>
                        <li><a href={`/${locale}/cli`} class="hover:text-[var(--primary)] hover:underline decoration-2 underline-offset-4 transition-all">{t('Footer.cli')}</a></li>
                    </ul>
                </div>

                {/* Resources */}
                <div>
                    <h4 class="font-black text-[var(--foreground)] mb-4 uppercase tracking-wider">{t('Footer.resources')}</h4>
                    <ul class="space-y-3 text-sm text-[var(--muted-foreground)] font-bold">
                        <li><a href={`/${locale}/docs`} class="hover:text-[var(--primary)] hover:underline decoration-2 underline-offset-4 transition-all">{t('Footer.docs')}</a></li>
                        <li><a href={`/${locale}/community`} class="hover:text-[var(--primary)] hover:underline decoration-2 underline-offset-4 transition-all">{t('Footer.community')}</a></li>
                        <li><a href={`/${locale}/blog`} class="hover:text-[var(--primary)] hover:underline decoration-2 underline-offset-4 transition-all">{t('Footer.blog')}</a></li>
                    </ul>
                </div>

                {/* Newsletter */}
                <div>
                    <h4 class="font-black text-[var(--foreground)] mb-4 uppercase tracking-wider">{t('Footer.stayUpdated')}</h4>
                    <p class="text-sm text-[var(--muted-foreground)] mb-4 font-medium">
                        {t('Footer.subscribeDesc')}
                    </p>
                    <div class="flex gap-2">
                        <input 
                            type="email" 
                            placeholder={t('Footer.emailPlaceholder')}
                            class="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-lg px-3 py-2 text-sm w-full focus:outline-none focus:ring-2 focus:ring-cyan-500/20 focus:border-cyan-500 transition-all"
                        />
                        <button aria-label={t('Footer.subscribeBtn') || 'Subscribe'} class="bg-cyan-600 hover:bg-cyan-700 text-white px-3 py-2 rounded-lg text-sm font-medium transition-colors">
                            →
                        </button>
                    </div>
                </div>
            </div>
        )}

        {/* Bottom */}
        <div class={`flex flex-col md:flex-row items-center justify-between gap-4 ${compact ? '' : 'pt-8 border-t border-slate-200 dark:border-slate-800'}`}>
            <div class="text-xs text-slate-500 dark:text-slate-500">
                © {currentYear} Killer-Skills Inc. {t('Footer.rights')}
            </div>
            <div class="flex gap-6 text-xs text-slate-500 dark:text-slate-500">
                <a href={`/${locale}/privacy`} class="hover:text-slate-900 dark:hover:text-white transition-colors">{t('Footer.privacy')}</a>
                <a href={`/${locale}/terms`} class="hover:text-slate-900 dark:hover:text-white transition-colors">{t('Footer.terms')}</a>
                <a href={`/${locale}/cookies`} class="hover:text-slate-900 dark:hover:text-white transition-colors">{t('Footer.cookie')}</a>
            </div>
        </div>
    </div>
</footer>
