---
import Layout from '../../../layouts/Layout.astro';
import MarketplaceSimplePage from '../../../components/MarketplaceSimplePage.astro';
import { SUPPORTED_LOCALES, type Locale } from '../../../i18n';

const { locale, topic } = Astro.params;
if (!locale || !SUPPORTED_LOCALES.includes(locale as Locale)) {
  return new Response(null, { status: 404, statusText: 'Not Found' });
}

const isZhCopy = locale.startsWith('zh');
const topicLabel = topic || 'workflow';
const title = isZhCopy ? `${topicLabel} 场景` : `${topicLabel} Use Case`;
const description = isZhCopy ? '相关 Skills、职业和分类入口。' : 'Related skills, occupations, and categories.';
---

<Layout locale={locale} title={title} description={description} appendBrand={false}>
  <MarketplaceSimplePage title={title} description={description}>
    <div class="marketplace-card-grid">
      <a class="marketplace-card-link" href={`/${locale}/skills?q=${encodeURIComponent(topicLabel)}`}>
        <h3>Skills</h3>
        <p>{isZhCopy ? '按关键词筛选目录。' : 'Filter the directory by keyword.'}</p>
      </a>
      <a class="marketplace-card-link" href={`/${locale}/occupations`}>
        <h3>{isZhCopy ? '职业' : 'Occupations'}</h3>
        <p>{isZhCopy ? '查看相关职业路径。' : 'Browse related role paths.'}</p>
      </a>
    </div>
  </MarketplaceSimplePage>
</Layout>
