---
import Layout from '../../../layouts/Layout.astro';
import MarketplaceSimplePage from '../../../components/MarketplaceSimplePage.astro';
import { SUPPORTED_LOCALES, type Locale } from '../../../i18n';

const { locale } = Astro.params;
if (!locale || !SUPPORTED_LOCALES.includes(locale as Locale)) {
  return new Response(null, { status: 404, statusText: 'Not Found' });
}

const isZhCopy = locale.startsWith('zh');
const title = isZhCopy ? 'IDE 集成' : 'IDE Integrations';
const description = isZhCopy
  ? '按 Claude Code、Cursor、Windsurf 等环境筛选可安装 Skills。'
  : 'Filter installable skills for Claude Code, Cursor, Windsurf, and related environments.';
const integrations = ['Claude Code', 'Cursor', 'Windsurf', 'VS Code'];
---

<Layout locale={locale} title={title} description={description} appendBrand={false}>
  <MarketplaceSimplePage title={title} description={description}>
    <div class="marketplace-card-grid">
      {
        integrations.map((name) => (
          <a class="marketplace-card-link" href={`/${locale}/skills?q=${encodeURIComponent(name)}`}>
            <h3>{name}</h3>
            <p>{isZhCopy ? '查看相关 Skills。' : 'View related skills.'}</p>
          </a>
        ))
      }
    </div>
  </MarketplaceSimplePage>
</Layout>
