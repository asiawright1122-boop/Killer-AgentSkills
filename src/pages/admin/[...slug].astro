---
import Layout from '../../layouts/Layout.astro';
import AdminApp from '../../islands/AdminApp';

export const prerender = false;

const apiBaseUrl = '/api/admin';
---

<Layout locale="en" title="Admin Dashboard" noindex={true} robots="noindex, nofollow">
  <AdminApp client:only="react" apiBaseUrl={apiBaseUrl} />
</Layout>
